# Codex-keygen 环境变量配置示例
# 常规场景无需手动复制：`keygen install` / `keygen config` 会自动生成 .env
# 本文件用于手动排障或高级自定义场景

# ── Web UI 监听地址 ──────────────────────────────────────────
# 监听主机（默认 0.0.0.0）
# APP_HOST=0.0.0.0

# 监听端口（建议与 runtime-config.json 保持一致，默认示例 1455）
# APP_PORT=1455

# Web UI 访问账号（默认 admin）
# APP_ACCESS_USERNAME=admin

# Web UI 访问密钥（默认 admin123，强烈建议修改）
# APP_ACCESS_PASSWORD=admin123

# ── 数据库 ───────────────────────────────────────────────────
# 本地 SQLite（默认，无需配置）
# APP_DATABASE_URL=data/database.db

# 远程 PostgreSQL（优先）
# APP_DATABASE_URL=postgresql://user:password@host:5432/dbname
